import React from 'react';
import type { ActionDefinition } from '../../../actions';
export function SlashCommandMenu({ actions, activeIndex, onSelect }: { actions: ActionDefinition[]; activeIndex: number; onSelect: (action: ActionDefinition) => void }) { if (!actions.length) return null; return <div role="listbox" aria-label="Quick actions" className="absolute bottom-[calc(100%+6px)] inset-x-0 z-50 max-h-64 overflow-auto rounded-lg border border-stone-200 bg-white p-1.5 shadow-lg">{(['built_in', 'workflow'] as const).map(kind => { const group = actions.filter(action => action.kind === kind); return group.length ? <div key={kind}><div className="px-2 py-1 text-[10px] font-semibold uppercase tracking-wide text-stone-400">{kind === 'built_in' ? 'Quick actions' : 'Workflows'}</div>{group.map(action => { const index = actions.indexOf(action); return <button role="option" aria-selected={index === activeIndex} key={action.id} type="button" onMouseDown={event => event.preventDefault()} onClick={() => onSelect(action)} className={`w-full rounded-md px-2 py-2 text-left ${index === activeIndex ? 'bg-[#eaf2f7]' : 'hover:bg-stone-50'}`}><span className="block text-xs font-semibold text-stone-900">{action.slashCommand}</span><span className="block text-[11px] text-stone-500">{action.description}</span></button>; })}</div> : null; })}<div className="px-2 pt-1 text-[10px] text-stone-400">↑↓ Select · Enter Confirm · Esc Close</div></div>; }
